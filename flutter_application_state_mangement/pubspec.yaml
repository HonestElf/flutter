name: flutter_application_state_mangement
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
    sdk: ">=2.18.4 <3.0.0"

dependencies:
    flutter:
        sdk: flutter

    cupertino_icons: ^1.0.2
    json_annotation: ^4.7.0

    provider: ^6.0.4

    flutter_riverpod: ^2.1.1
    hooks_riverpod: ^2.1.1
    flutter_hooks: ^0.18.5+1

    flutter_bloc: ^8.1.1

    redux: ^5.0.0
    flutter_redux: ^0.10.0

    mobx: ^2.1.3
    flutter_mobx: ^2.0.6+5

    fish_redux: ^0.3.7

dev_dependencies:
    flutter_test:
        sdk: flutter

    flutter_lints: ^2.0.0
    json_serializable: ^6.5.4
    build_runner: ^2.3.2
    import_sorter: ^4.6.0
    mobx_codegen: ^2.1.1

flutter:
    uses-material-design: true
    assets:
        - assets/images/
